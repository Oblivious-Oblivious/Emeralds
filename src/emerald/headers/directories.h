#ifndef __DIRECTORIES_H_
#define __DIRECTORIES_H_

#include "os_definitions.h"

/**
 * @func: make_directory
 * @brief A cross platform implementation of creating a directory
 * @param name -> The name of the directory to create
 */
void make_directory(char *name);

#endif
